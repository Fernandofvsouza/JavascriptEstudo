//String, number, undefined, null ,boolean


const nome = 'Luiz';//string
const nome1 = "Fernando";//string
const nome2 = `Pedro`;//string

const num1 = 10; //number
const num2 = 20.52; //number

let nomeAluno; //undefined = não aponta pra local nenhum na memoria

const sobrenomeAluno = null; //Nulo - não aponta pra local nenhum na memoria, usamos quando queremos disconfigurar uma variavel


const aprovado = true; //booleano -- verdadeiro ou falso (valor logico);
const reprovado = false; //booleano -- verdadeiro ou falso (valor logico);

// verificar o tipo
console.log(typeof(aprovado));


